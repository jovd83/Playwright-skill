interface:
  display_name: "Root Cause Report"
  short_description: "Analyze Playwright failures and explain the likely cause"
  default_prompt: "Use $playwright-documentation-root-cause to investigate this failing Playwright run and produce a concise root-cause report."
